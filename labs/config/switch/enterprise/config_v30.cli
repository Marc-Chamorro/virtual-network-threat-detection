hostname switch-ADMIN

vlan 30
   name VLAN30

interface eth1
    switchport mode access
    switchport access vlan 30
    no shutdown

interface eth2
    switchport mode access
    switchport access vlan 30
    no shutdown
