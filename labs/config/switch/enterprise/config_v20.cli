hostname switch-vlan20

vlan 20
   name VLAN20

interface eth1
    switchport mode access
    switchport access vlan 20
    no shutdown

interface eth2
    switchport mode access
    switchport access vlan 20
    no shutdown

interface eth3
    switchport mode access
    switchport access vlan 20
    no shutdown

interface eth4
    switchport mode access
    switchport access vlan 20
    no shutdown

