name: test

topology:
  nodes:
    internal_server:
      kind: linux
      image: server_vntd:latest
      env: 
        SSH_SERVER: 1
        #ENABLE_DHCP: 1
        #DHCP_INTERFACE: eth1
      # binds:
       # - ./config/dhcp/dhcpd.conf:/etc/dhcp/dhcpd.conf