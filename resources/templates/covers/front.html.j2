<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body {
      font-family: sans-serif;
      text-align: center;
      margin-top: 30%;
    }
    h1 {
      font-size: 42px;
      margin-bottom: 10px;
    }
    h2 {
      font-weight: normal;
      color: #666;
    }
  </style>
</head>
<body>
  <h1>{{ config.site_name }}</h1>
  <h2>{{ config.site_description }}</h2>
  <p>Author: {{ config.site_author }}</p>
  <p>{{ build_date }}</p>
</body>
</html>
